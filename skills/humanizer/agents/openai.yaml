interface:
  display_name: "Humanizer"
  short_description: "Write and edit in the user's real voice"
  default_prompt: "Use $humanizer to rewrite this in my demonstrated voice without changing its meaning."
policy:
  allow_implicit_invocation: true
