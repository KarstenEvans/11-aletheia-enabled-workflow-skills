interface:
  display_name: "Sales Followup"
  short_description: "Turn calls into accurate follow-up actions"
  default_prompt: "Use $sales-followup to turn this call into an email, factual notes, and owned next steps."
policy:
  allow_implicit_invocation: true
